# Bootstrap Hosts Inventory
#
# This inventory file is used for one-time onboarding of new hosts.
# Copy this file to bootstrap-hosts.ini and add your hosts.
#
# Usage:
#   ansible-playbook playbooks/bootstrap-host.yml -i inventory/bootstrap-hosts.ini --ask-pass --ask-become-pass
#
# After bootstrap, move hosts to inventory/hosts for normal management.

[bootstrap_hosts]
# Add hosts that need to be bootstrapped here
#
# Format options:
#
# 1. With password authentication (will prompt for password):
#    hostname.example.com ansible_host=192.168.1.100 ansible_user=ubuntu
#
# 2. With SSH key already configured:
#    hostname.example.com ansible_host=192.168.1.100 ansible_user=ubuntu
#
# 3. With password in inventory (less secure, not recommended):
#    hostname.example.com ansible_host=192.168.1.100 ansible_user=ubuntu ansible_ssh_pass=password
#
# 4. With custom SSH key:
#    hostname.example.com ansible_host=192.168.1.100 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/custom_key

# Example entries (uncomment and modify):
# new-server1.example.com ansible_host=192.168.1.100 ansible_user=ubuntu
# new-server2.example.com ansible_host=192.168.1.101 ansible_user=admin

[bootstrap_hosts:vars]
# Default variables for bootstrap hosts
ansible_python_interpreter=/usr/bin/python3

# Bootstrap-specific variables
# bootstrap_ansible_user: ansible  # User to create for Ansible management (default: ansible)
# bootstrap_ssh_public_key: "{{ lookup('file', '~/.ssh/id_rsa.pub') }}"  # SSH key to install
# bootstrap_disable_password_auth: false  # Set to true to disable password auth after bootstrap
